{
  "name": "matador-backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "setup": "yarn && cd front-end/ && yarn && yarn build && cd .. && node app.js",
    "lint": "eslint '*.js' --fix && eslint './front-end/src/**/*.js' --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "lint-staged": "^8.0.4"
  },
  "devDependencies": {
    "husky": "^1.1.3",
    "prettier": "^1.15.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
